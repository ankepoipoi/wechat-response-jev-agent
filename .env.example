# Echo 配置模板：复制为 .env 后填入自己的 Key

# Jev（TypeSafe）第一层判断：情绪、意图、互动温度。必需。
TYPESAFE_API_KEY=

# 服务端口与监听地址。HOST 保持 127.0.0.1，只对本机开放。
PORT=3199
HOST=127.0.0.1

# 单次分析的最大条数。更长的对话会自动只分析最近这么多条（不会失败）。
# 调大能覆盖更长的历史，但请求体也更大，注意别顶爆模型上下文。
MAX_MESSAGES=120
